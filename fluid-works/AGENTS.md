# Fluid Works project instructions

- Treat source updates as GitHub-source changes only.
- Do not run Gradle, edit Gradle wrappers, trigger CI, or produce/export JARs unless Alex explicitly requests that exact action.
- Never remove an existing gameplay feature while adding another feature.
